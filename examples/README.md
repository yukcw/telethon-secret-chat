# Examples

This directory contains examples for `telethon-secret-chat`.

## Setup

These examples rely on the local version of `telethon-secret-chat`.
Dependencies are managed via `uv`.

```bash
cd examples
# This will install dependencies including the local package and boto3
uv sync
```

## Running Examples

```bash
uv run aws_dynamodb_example.py
```
